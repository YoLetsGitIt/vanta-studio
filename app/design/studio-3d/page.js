import StudioJourney from '@/components/studio-journey/StudioJourney';

export const metadata = {
  title: 'Step inside — Vanta Studio 3D preview',
  robots: { index: false, follow: false },
};

export default function StudioJourneyPreview() {
  return <StudioJourney />;
}
