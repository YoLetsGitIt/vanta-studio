export const metadata = {
  title: 'Studio booking',
};

export default function StudioBookingLayout({ children }) {
  return <div data-theme="dark" style={{ colorScheme: 'dark' }}>{children}</div>;
}
